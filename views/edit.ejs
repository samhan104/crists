<!DOCTYPE html>
<html lang="en">

<head>
    <%- include ('./partials/head.ejs') %>
        <title>Your Crists</title>
</head>

<body>
    <div class="container">
    <div class="pageTitle">
        <h1><%=list.name%></h1>
    </div>
    <div class="content">
        <div id="small">
                <form action="/<%=list.id%>?_method=PUT" method="POST">
                    Name: <input type="text" name="name" value="<%=list.name%>"><br>
                    Items: <% for (i=0; i < list.items.length; i++) { %>
                        <input type="text" name="items" value="<%=list.items[i]%>"><br>
                        <% } %>
                            <input type="submit" value="Submit Changes">
                </form>
        </div>
    </div>
    <div class="addButton">
        <a href="/<%=list.id%>"><button>Back</button></a>
    </div>
</div>
</body>

</html>